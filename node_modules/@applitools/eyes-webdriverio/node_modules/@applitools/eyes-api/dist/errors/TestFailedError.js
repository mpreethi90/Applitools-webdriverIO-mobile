"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TestFailedError = void 0;
const utils = require("@applitools/utils");
const EyesError_1 = require("./EyesError");
const TestResults_1 = require("../output/TestResults");
class TestFailedError extends EyesError_1.EyesError {
    constructor(message, results) {
        if (!utils.types.isString(message)) {
            results = message;
            message = `Test '${results.name}' of '${results.appName}' is failed! See details at ${results.url}`;
        }
        super(message);
        if (results)
            this._results = results;
    }
    get testResults() {
        return this._results;
    }
    getTestResults() {
        return new TestResults_1.TestResultsData(this._results);
    }
}
exports.TestFailedError = TestFailedError;
